import type { CapacitorConfig } from "@capacitor/cli";
const config: CapacitorConfig = { appId: "com.nujoka.weatherstation", appName: "Abuja Weather Station", webDir: "dist" };
export default config;

